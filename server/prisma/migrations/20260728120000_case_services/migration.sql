-- Ключи услуг кейса. Пустой массив по умолчанию, чтобы существующие строки
-- остались валидными: связи проставляются следом сидом из снапшота.
ALTER TABLE "cases" ADD COLUMN "services" TEXT[] DEFAULT ARRAY[]::TEXT[] NOT NULL;
